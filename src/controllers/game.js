// const GameScene = require('e-civ-game')

const Game = async (ctx) => {
  // let data
  // try {
  //   if (typeof ctx.request.body === 'object') {
  //     data = ctx.request.body
  //   } else {
  //     data = JSON.parse(ctx.request.body)
  //   }
  // } catch (e) {
  //   throw new Error(e)
  // }

  // const {own, enemy} = data
  // const game = new GameScene()
  // game.setClientOrServer(GameScene.SERVER)
  // game.makeScene('gameOver', () => {})
  // game.overScene('gameOver', null, null, (result, scene, bg) => {
  //   ctx.body = bg.actionFlows
  // })
  // game.load(() => {
  //   game.setSoilders(enemy, own)
  //   game.start(own.user)
  // })
}

module.exports = {
  Game
}
